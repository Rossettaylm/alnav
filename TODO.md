# loggrep P1 需求清单

面向 AI agent 日志分析场景的增强功能。

## P1-1: `--since`/`--until` 支持完整日期 ⬜

**痛点**: 当前只支持 `HH:MM:SS`，xlog 格式时间戳是 `YYYY-MM-DD HH:MM:SS`，跨天日志无法按日期筛选。

**方案**: 兼容两种写法：
- `--since 10:30:00` （仅时间，现有行为）
- `--since "2026-03-04 10:30:00"` （完整日期时间）

## P1-2: `--pid`/`--tid` 过滤 ⬜

**痛点**: AI 追踪某个线程的执行流时，只能用 `--msg` 间接匹配，无法按 PID/TID 精确过滤。

**方案**: 新增 `--pid` 和 `--tid` 参数，支持数值精确匹配或正则。

## P1-3: 时间窗口聚合 `--summary --bucket` ⬜

**痛点**: AI 无法判断错误是集中爆发还是均匀分布，缺少时间维度分布信息。

**方案**: `--summary` 模式下可选 `--bucket 1m`，按时间桶输出每段的 level 分布。

## P1-4: `--fields` 字段选择 ⬜

**痛点**: AI token 有限，PID/TID 等字段对多数分析无用，需要裁剪输出节省 token。

**方案**: `--fields timestamp,level,tag,msg` 只输出指定字段（text/json/csv 均生效）。

## P1-5: 多文件时间线合并排序 ⬜

**痛点**: 多个日志文件按 glob 读入时是顺序拼接，不是按时间交叉排序。

**方案**: `-f '*.log' --sort-time` 按 timestamp 归并排序输出。
